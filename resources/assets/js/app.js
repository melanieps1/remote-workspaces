// Vue Stuff

window.Vue = require('vue');

// Vue.component('resultsCard', require('./components/resultsCard.vue'));

const app = new Vue({
    
    el: '#app',

    components: {
    	//
    }
    
});